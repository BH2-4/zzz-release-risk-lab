---
phase: ZZZ-02-fixed-case-evidence-bound-ai-compilation
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
autonomous: false
files_modified:
  - experiments/output/ai/compiled-scenario-v1.json
  - research/model-card.md
  - research/README.md
  - .planning/phases/ZZZ-02-fixed-case-evidence-bound-ai-compilation/02-VALIDATION.md
requirements: [AI-01, AI-02, AI-03, AI-04]
user_setup:
  - service: MiniMax
    why: "AI-01 requires exactly one user-authorized real MiniMax M2.7 request after 02-01 is green."
    env_vars:
      - PROGRAM_E_AI_BASE_URL
      - PROGRAM_E_AI_API_KEY
      - PROGRAM_E_AI_MODEL
    instructions:
      - "Start or resume the executor from a shell/session that already exports the three variables, or inject them through an approved Codex execution environment."
      - "Use https://api.minimaxi.com/v1 and MiniMax-M2.7 for the endpoint and model variables."
      - "Do not paste a credential into chat or store it in a repository file; the executor checks inherited name presence only."
must_haves:
  truths:
    - "D-11: 02-01-SUMMARY.md and 02-VALIDATION.md prove focused plus build/Node/E2E pre-live gates green before the human checkpoint; no live work begins otherwise."
    - "The executor inherits PROGRAM_E_AI_BASE_URL, PROGRAM_E_AI_API_KEY, and PROGRAM_E_AI_MODEL from its starting/resumed process and checks only name presence, never reading, sourcing, listing, parsing, hashing, printing, or persisting any .env.* file or credential value."
    - "D-04 through D-07 and AI-01: after explicit human authorization, exactly one `npm run compile:scenario` invocation issues at most one MiniMax request; there is no provider retry, repair call, fallback, replay, or second attempt after any failure."
    - "D-01 through D-03 and AI-02/AI-03: the accepted disk artifact revalidates against current canonical inputs with MiniMax provider/model, non-empty body request ID, actual optional trace ID only if returned, complete source digests, frozen Theory System ID, passed validation, realModelUsed=true, and exhaustive approved bindings."
    - "D-08 through D-10 and AI-04: the artifact and bounded docs preserve verified 1.4 history, verified 3.1 product context, synthetic 3.1 fade risk, neutral region factors, and exact outfit direct zh-CN support with direct en/direct ja unavailable; no translation promotion or probability claim appears."
    - "D-13: after disk revalidation and bounded docs, npm run build, npm test, and npm run test:e2e all pass again on the current Mac; ASVS L2 has no unresolved HIGH/CRITICAL threat and an independent review reports BLOCKER=0, HIGH=0, MEDIUM=0."
    - "No chain, wallet, contract, signing, deployment, network-switch, transaction, push, PR, or cross-platform action occurs in this plan."
  artifacts:
    - path: experiments/output/ai/compiled-scenario-v1.json
      provides: "The sole accepted attributable live-model artifact for the frozen 1.4/3.1 slice"
    - path: research/model-card.md
      provides: "Bounded live compilation status and unchanged evidence/model limitations"
    - path: research/README.md
      provides: "Research index entry for the accepted artifact and its digest/provenance"
    - path: .planning/phases/ZZZ-02-fixed-case-evidence-bound-ai-compilation/02-VALIDATION.md
      provides: "One-attempt, disk-validation, post-live gate, ASVS L2, and independent-review evidence"
  key_links:
    - from: .planning/phases/ZZZ-02-fixed-case-evidence-bound-ai-compilation/02-01-SUMMARY.md
      to: scripts/compile-scenario.js
      via: "green credential-free implementation and pre-live gates are a hard precondition"
      pattern: "build|npm test|test:e2e|no live"
    - from: scripts/compile-scenario.js
      to: experiments/output/ai/compiled-scenario-v1.json
      via: "one authorized request and the established parent-flock publication path"
      pattern: "MiniMax-M2.7|live-model|requestId"
    - from: experiments/output/ai/compiled-scenario-v1.json
      to: src/compilation-pipeline.js
      via: "disk bytes are loaded and passed to validateFixedCompilationArtifact against current checked inputs"
      pattern: "validateFixedCompilationArtifact|sourceDigests"
    - from: experiments/output/ai/compiled-scenario-v1.json
      to: research/model-card.md
      via: "docs update only after accepted bytes revalidate"
      pattern: "live-model|synthetic-counterfactual|direct evidence"
---

<objective>
After the complete offline plan is independently green, obtain inherited MiniMax configuration through a human checkpoint, execute exactly one authorized live compilation, revalidate the persisted bytes, update only bounded status documentation, and close Phase 2 with post-live macOS gates, ASVS L2, and independent review.

Purpose: satisfy the live evidence portion of AI-01 through AI-04 without widening credential, retry, evidence, filesystem, or chain authority.
Output: one validated live artifact, bounded model/research status, post-live gate/security/review evidence, and a Phase 2 completion summary.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/ZZZ-02-fixed-case-evidence-bound-ai-compilation/02-CONTEXT.md
@.planning/phases/ZZZ-02-fixed-case-evidence-bound-ai-compilation/02-RESEARCH.md
@.planning/phases/ZZZ-02-fixed-case-evidence-bound-ai-compilation/02-AI-SPEC.md
@.planning/phases/ZZZ-02-fixed-case-evidence-bound-ai-compilation/02-VALIDATION.md
@.planning/phases/ZZZ-02-fixed-case-evidence-bound-ai-compilation/02-01-SUMMARY.md
@research/model-card.md
@research/README.md
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: Inherit MiniMax variables and authorize exactly one live request</name>
  <precondition>Plan 02-01 completed with its focused suites and all three pre-live Mac gates green, and its summary explicitly states that no credential or live request was used.</precondition>
  <read_first>
    - @AGENTS.md
    - @.planning/phases/ZZZ-02-fixed-case-evidence-bound-ai-compilation/02-CONTEXT.md
    - @.planning/phases/ZZZ-02-fixed-case-evidence-bound-ai-compilation/02-01-SUMMARY.md
    - @.planning/phases/ZZZ-02-fixed-case-evidence-bound-ai-compilation/02-VALIDATION.md
  </read_first>
  <files></files>
  <action>Pause before any live command. Ask the user to start or resume the executor from a shell/session in which `PROGRAM_E_AI_BASE_URL`, `PROGRAM_E_AI_API_KEY`, and `PROGRAM_E_AI_MODEL` are already exported, or to inject them through an approved Codex execution environment, and to authorize exactly one live compilation request. Do not ask for a credential in chat. After resumption, check only that the three names exist in the inherited `process.env`; emit only a boolean success/failure outcome, never values. Do not locate, list, open, source, parse, print, hash, copy, stage, or commit `.env.*`. If inheritance is unavailable, a name is absent, pre-live evidence is incomplete, or authorization is ambiguous, keep the checkpoint unresolved and make no provider request. No automated action may occur between successful checkpoint resolution and Task 2's single compile command.</action>
  <instructions>From a shell/session with the three variables already exported, start or resume this executor, then explicitly authorize one `npm run compile:scenario` request. Keep the API key outside chat and repository files.</instructions>
  <verification>The executor checks the inherited environment name set only and records that explicit one-request authorization was received. Missing inheritance leaves the checkpoint unresolved with zero live calls.</verification>
  <resume-signal>After starting or resuming with the inherited names, type "authorize one live request"; type "not available" if inheritance cannot be provided.</resume-signal>
  <verify>
    <automated>node -e "const n=['PROGRAM_E_AI_BASE_URL','PROGRAM_E_AI_API_KEY','PROGRAM_E_AI_MODEL'];process.exit(n.every(k=&gt;Object.prototype.hasOwnProperty.call(process.env,k))?0:1)"</automated>
  </verify>
  <acceptance_criteria>All three names are inherited without disclosure, 02-01 evidence is green, and the user has authorized exactly one request; otherwise the plan stops before Task 2.</acceptance_criteria>
  <done>The trusted process, not a file or browser, holds the required configuration and has one-request authorization.</done>
</task>

<task type="auto">
  <name>Task 2: Execute once, revalidate disk bytes, and update bounded documentation</name>
  <precondition>Task 1 resolved in this executor process with inherited variable-name presence and explicit authorization; no intervening automated action occurred.</precondition>
  <read_first>
    - @AGENTS.md
    - @.planning/phases/ZZZ-02-fixed-case-evidence-bound-ai-compilation/02-AI-SPEC.md
    - @src/compilation-pipeline.js
    - @scripts/compile-scenario.js
    - @data/evidence/extractions/zzz-1-4-fade-approved.json
    - @data/evidence/reviews/zzz-1-4-fade-evidence-review.json
    - @data/evidence/ledger.json
    - @data/evidence/theory-catalog.json
    - @data/scenarios/zzz-3-1-fade-risk-vertical-slice.json
    - @experiments/output/theory/zzz-1-4-fade-run.json
    - @research/model-card.md
    - @research/README.md
  </read_first>
  <files>experiments/output/ai/compiled-scenario-v1.json, research/model-card.md, research/README.md</files>
  <action>Run `npm run compile:scenario` exactly once. This command is the only authorized live invocation in the plan. If transport, timeout, provider status, response parsing, body ID, fixed input, evidence, theory, numeric, region, language, provenance, validation, cooperative-writer busy/conflict, recovery, or publication fails, stop immediately with the sanitized error. Do not repair output, rerun the command, retry the provider, fall back to another model or fixture, replay a recording, or issue a second request; obtaining another attempt requires a new explicit user decision in a new execution. Do not invoke the Theory Agent live path or any chain path.

After a zero exit, load `experiments/output/ai/compiled-scenario-v1.json` from disk and call `validateFixedCompilationArtifact` with the current approved extractions, evidence review, ledger, theory catalog, authoritative Theory run/System, and vertical slice. Require exact fixed IDs, `provenance.mode=live-model`, MiniMax provider/model, non-empty body request ID, optional trace ID only if returned, canonical source digests, passed validation, `realModelUsed=true`, approved citations/mappings/mechanisms/numeric bindings, neutral region factors, and explicit synthetic-counterfactual wording. Recheck that the exact Lucy outfit claim is direct zh-CN only with direct en/direct ja unavailable and that English support for other approved claims was not erased. Compute the repository canonical digest from the disk-loaded object without printing prompts, headers, raw response, reasoning, or credentials. If disk validation fails, do not update docs and do not make another request.

Only after disk validation passes, update `research/model-card.md` and `research/README.md` with the accepted artifact path/digest, provider/model/body request ID, actual optional trace ID if present, source/Theory authority, and validation status. Preserve the distinctions among verified 1.4 history, verified 3.1 product context, synthetic 3.1 fade risk, and scenario risk rather than reality probability. State the exact outfit boundary as direct zh-CN with direct en/direct ja unavailable; do not promote translation or contextual pages and do not invalidate other approved English evidence. Preserve that the Theory Agent upstream remains a deterministic fixture. Add no generalized-case, hybrid-Agent, cross-platform, chain-completion, player-sample, or real-world probability claim.</action>
  <verify>
    <automated>node -e "const {digestValue}=require('./src/artifact-digest.js');const {validateFixedCompilationArtifact}=require('./src/compilation-pipeline.js');const a=require('./data/evidence/extractions/zzz-1-4-fade-approved.json');const r=require('./data/evidence/reviews/zzz-1-4-fade-evidence-review.json');const l=require('./data/evidence/ledger.json');const c=require('./data/evidence/theory-catalog.json');const s=require('./data/scenarios/zzz-3-1-fade-risk-vertical-slice.json');const run=require('./experiments/output/theory/zzz-1-4-fade-run.json');const out=require('./experiments/output/ai/compiled-scenario-v1.json');const inputs={approvedExtractions:a,evidenceReview:r,ledger:l,theoryCatalog:c,theoryRun:run,theorySystem:run.theorySystem,verticalSlice:s};const v=validateFixedCompilationArtifact(out,inputs);const live=v.valid&amp;&amp;out.provenance?.mode==='live-model'&amp;&amp;out.capabilities?.realModelUsed===true&amp;&amp;out.provenance?.provider==='minimax'&amp;&amp;out.provenance?.model==='MiniMax-M2.7'&amp;&amp;typeof out.provenance?.requestId==='string'&amp;&amp;out.provenance.requestId.trim().length&gt;0;if(!live)throw new Error('disk artifact live validation failed');process.stdout.write(digestValue(out)+'\n')"</automated>
  </verify>
  <acceptance_criteria>The sole authorized command produced one disk artifact that revalidates against current canonical inputs, and documentation was updated only from that validated artifact with all evidence and scope boundaries intact.</acceptance_criteria>
  <done>AI-01 has one attributable live-request artifact, while AI-02 through AI-04 remain enforced on persisted bytes and bounded public claims.</done>
</task>

<task type="auto">
  <name>Task 3: Run post-live gates, close ASVS L2, and obtain independent review</name>
  <precondition>Task 2 succeeded once, the persisted artifact revalidates from disk, and bounded documentation is complete.</precondition>
  <read_first>
    - @AGENTS.md
    - @.planning/phases/ZZZ-02-fixed-case-evidence-bound-ai-compilation/02-VALIDATION.md
    - @.planning/phases/ZZZ-02-fixed-case-evidence-bound-ai-compilation/02-AI-SPEC.md
    - @.planning/phases/ZZZ-02-fixed-case-evidence-bound-ai-compilation/02-01-SUMMARY.md
    - @experiments/output/ai/compiled-scenario-v1.json
    - @research/model-card.md
    - @research/README.md
    - @package.json
    - @playwright.config.js
  </read_first>
  <files>.planning/phases/ZZZ-02-fixed-case-evidence-bound-ai-compilation/02-VALIDATION.md</files>
  <action>Run the post-live current-Mac gates in the fixed order `npm run build`, `npm test`, and `npm run test:e2e`, then `git diff --check`, a filename-bounded secret/reasoning scan of Phase 2 artifact/docs/validation files, and the same disk-artifact revalidation from Task 2. Record exact commands, exit statuses, test counts, artifact digest/provenance, one-attempt evidence, platform, language boundary, and any findings in `02-VALIDATION.md`; missing, skipped, flaky, or red evidence blocks completion.

Perform an OWASP ASVS Level 2 review across inherited environment handling, MiniMax transport, provider normalization, fixed compiler validation, source-digest authority, cooperative parent-flock publication, disk artifact, documentation, and completion evidence. Resolve every HIGH/CRITICAL threat before proceeding; accepted LOW/MEDIUM items require case-specific disposition, and the repository's non-cooperating same-UID writer limitation remains an explicit residual risk rather than an absolute safety claim. Then obtain a fresh independent deep review of the complete Phase 2 code, tests, live artifact, docs, validation record, AI-01 through AI-04, and all D-01 through D-13 boundaries. Executor self-review does not count. Any independent BLOCKER, HIGH, or MEDIUM finding returns to implementation and invalidates completion until fixed and all affected gates/artifact checks are rerun. Do not perform another provider request during remediation; if a fix would invalidate the accepted artifact, stop for a new user decision. Do not push, open a PR, read `.env.*`, or perform a wallet/contract/network/signing/transaction action.</action>
  <verify>
    <automated>npm run build &amp;&amp; npm test &amp;&amp; npm run test:e2e &amp;&amp; git diff --check &amp;&amp; node -e "const {validateFixedCompilationArtifact}=require('./src/compilation-pipeline.js');const a=require('./data/evidence/extractions/zzz-1-4-fade-approved.json');const r=require('./data/evidence/reviews/zzz-1-4-fade-evidence-review.json');const l=require('./data/evidence/ledger.json');const c=require('./data/evidence/theory-catalog.json');const s=require('./data/scenarios/zzz-3-1-fade-risk-vertical-slice.json');const run=require('./experiments/output/theory/zzz-1-4-fade-run.json');const out=require('./experiments/output/ai/compiled-scenario-v1.json');const inputs={approvedExtractions:a,evidenceReview:r,ledger:l,theoryCatalog:c,theoryRun:run,theorySystem:run.theorySystem,verticalSlice:s};const v=validateFixedCompilationArtifact(out,inputs);const live=v.valid&amp;&amp;out.provenance?.mode==='live-model'&amp;&amp;out.capabilities?.realModelUsed===true&amp;&amp;out.provenance?.provider==='minimax'&amp;&amp;out.provenance?.model==='MiniMax-M2.7'&amp;&amp;typeof out.provenance?.requestId==='string'&amp;&amp;out.provenance.requestId.trim().length&gt;0;if(!live)process.exit(1)"</automated>
    <manual>Confirm `02-VALIDATION.md` records the single authorized live attempt, green post-live build/Node/E2E gates, disk revalidation, ASVS L2 with no unresolved HIGH/CRITICAL, and an independent report with BLOCKER=0, HIGH=0, MEDIUM=0.</manual>
  </verify>
  <acceptance_criteria>All post-live gates and disk checks are green, ASVS L2 is closed at the stated threshold, independent review has no BLOCKER/HIGH/MEDIUM, and no second provider or chain action occurred.</acceptance_criteria>
  <done>Phase 2 has complete live-artifact, deterministic-validation, evidence-language, filesystem, gate, security, and independent-review evidence for AI-01 through AI-04.</done>
</task>

</tasks>

<threat_model>
## Live Security Contract

- Standard: OWASP ASVS Level 2 across the complete live data flow; unresolved HIGH or CRITICAL blocks completion, and D-13 independently blocks on any BLOCKER/HIGH/MEDIUM review finding.
- Request authorization: inherited names plus explicit human authorization permit one command and at most one provider request. Any failure ends the plan attempt without retry.
- Credential boundary: trusted Node only; no `.env.*` access, browser exposure, value echo, prompt/raw/reasoning persistence, wallet access, or chain authority.
- Publication boundary: the 02-01 established parent-directory flock/helper contract remains unchanged; busy/conflict fails without a second provider call.
- Evidence boundary: exact outfit direct zh-CN only, direct en/ja unavailable; translation/context cannot close the gap.

| Threat | Severity | Live disposition |
|--------|----------|------------------|
| Provider/provenance spoofing | high | Exact provider/model, body id, optional actual trace id, live mode, current source digests, disk revalidation. |
| Artifact/model tampering | critical | Fixed inputs before call, closed validation before write, current-input validation after disk read. |
| Credential/reasoning disclosure | critical | Inherited process only, sanitized errors, bounded file scan, no raw/prompt/reasoning persistence. |
| Repudiation/extra request | high | Human checkpoint, one command, fetch-count regressions, request id and artifact digest, no retry. |
| External failure/DoS | medium | Timeout, one parse, one attempt, no partial accepted artifact, new decision required after failure. |
| Cooperative publication contention | high | Existing non-blocking parent flock and identity conflict contract; no provider retry after busy/conflict. |
| Unsupported language/region claim | critical | Disk validator and documentation review retain direct-source and neutral-region rules. |
| Chain/tool elevation | high | No tools, wallet, contract, network switch, signature, deployment, or transaction. |
</threat_model>

<verification>
1. 02-01 summary and validation evidence establish credential-free implementation plus green pre-live gates.
2. The inherited-environment checkpoint resolves immediately before the sole live command and never inspects `.env.*` or prints values.
3. One `npm run compile:scenario` command either succeeds once or terminates the attempt; no retry, repair, fallback, replay, or second request exists.
4. Persisted bytes revalidate against every current canonical input before documentation changes.
5. Documentation records accepted provenance and preserves historical/product/synthetic, language, region, fixture-Theory-Agent, and non-probability boundaries.
6. Post-live build, Node, and E2E gates plus diff/scan/disk checks are green on the current Mac.
7. ASVS L2 and a separate independent review meet their respective zero-open-finding thresholds before completion.
</verification>

<success_criteria>
- AI-01: one trusted Node request produced the sole attributable MiniMax live artifact without browser/artifact credential exposure.
- AI-02: disk validation proves exhaustive approved claim, theory, mechanism, numeric, region, and synthetic-assumption binding.
- AI-03: disk provenance includes provider, model, body request ID, actual optional trace ID, source digests, frozen Theory System ID, and passed validation.
- AI-04: zh-CN/en/ja remain review targets, while direct support remains source-specific and the exact outfit sentence is direct zh-CN only with direct en/ja unavailable.
- Usage remains OPT-OUT. There is no provider retry, `.env.*` read, generalized case, chain action, hybrid-Agent claim, probability claim, or cross-platform claim.
- Pre-live and post-live gates, ASVS L2, and independent review satisfy D-13 without waiver.
</success_criteria>

## Artifacts This Plan Produces

- One validated live artifact at `experiments/output/ai/compiled-scenario-v1.json`.
- Bounded accepted-artifact status in `research/model-card.md` and `research/README.md`.
- Complete live-attempt, disk, post-live gate, ASVS L2, and independent-review evidence in `02-VALIDATION.md`.
- Executor summary at `.planning/phases/ZZZ-02-fixed-case-evidence-bound-ai-compilation/02-02-SUMMARY.md`.
- No code change, evidence-ledger mutation, Theory System regeneration, package change, browser credential path, chain state, push, PR, or cross-platform claim.

## Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | - | One auditable real-model artifact for frozen 1.4/3.1 slice | 02-02 | COVERED | Checkpoint, one request, disk revalidation, closure. |
| REQ | AI-01 | One real OpenAI-compatible request with Node-only BYOK | 02-01 + 02-02 | COVERED | Offline path first; one authorized live command here. |
| REQ | AI-02 | Bind every claim, theory, number, and region | 02-01 + 02-02 | COVERED | Validators in 02-01; disk proof here. |
| REQ | AI-03 | Provider/model/request/source/Theory/validation provenance | 02-01 + 02-02 | COVERED | Closed contract then persisted evidence. |
| REQ | AI-04 | Three-language review without translation promotion | 02-01 + 02-02 | COVERED | Direct-language validator plus artifact/docs review. |
| RESEARCH | - | M2.7 body id, optional trace id, reasoning boundary | 02-01 + 02-02 | COVERED | Implemented offline and verified in accepted artifact. |
| RESEARCH | - | Exact outfit direct zh-CN; direct en/ja unavailable | 02-01 + 02-02 | COVERED | Disk and docs must retain exact boundary. |
| API | - | Usage metadata | 02-01 + 02-02 | OPT-OUT | Not parsed, persisted, required, or used for acceptance. |
| CONTEXT | D-01..D-10 | Fixed provider/evidence/provenance/language contract | 02-01 + 02-02 | COVERED | Implemented offline, revalidated live. |
| CONTEXT | D-11 | Offline first; inherited environment; no .env.* | 02-01 + 02-02 | COVERED | Serial dependency and Task 1 checkpoint. |
| CONTEXT | D-12 | Fixture cannot satisfy AI-01 | 02-01 + 02-02 | COVERED | Only this accepted live artifact closes AI-01. |
| CONTEXT | D-13 | Live artifact, gates, ASVS, independent review | 02-02 | COVERED | Tasks 2-3. |

<output>
Create `.planning/phases/ZZZ-02-fixed-case-evidence-bound-ai-compilation/02-02-SUMMARY.md` after execution. Mark Phase 2 complete only when the disk artifact remains valid, pre/post Mac gates are green, ASVS L2 has no unresolved HIGH/CRITICAL, independent review reports BLOCKER=0/HIGH=0/MEDIUM=0, and exactly one authorized live request occurred.
</output>
