---
phase: ZZZ-01-trustworthy-theory-pipeline
plan: "04"
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - .planning/phases/ZZZ-01-trustworthy-theory-pipeline/01-VALIDATION.md
autonomous: true
requirements: [TRUST-01, TRUST-02, TRUST-03, TRUST-04]
must_haves:
  truths:
    - "D-10: focused security tests, npm run build, complete npm test, and npm run test:e2e all exit 0 at the phase boundary."
    - "D-11: no expected-failure waiver exists; all six original CLI regressions remain green and the locked test retains baseline SHA-256 53ca63c48afb367691caa492cb478823575473e922719e9cafd63c97effe8e23."
    - "D-12 and D-13: the closure evidence targets the competition macOS machine and records that independent review plus all three local gates precede any feature-branch push or PR."
    - "D-14: filesystem claims are bounded to cooperative local writers using the parent-directory flock; the same-UID non-cooperative residual risk is stated without absolute safety language."
    - "The regenerated run passes READY lineage/provenance reconstruction and the repository diff has no unexpected secret, unsafe claim, or whitespace defect."
    - "ASVS L2 review has no unresolved HIGH-or-above finding; any HIGH finding blocks phase completion."
  artifacts:
    - path: .planning/phases/ZZZ-01-trustworthy-theory-pipeline/01-VALIDATION.md
      provides: "command-by-command closure evidence and final Nyquist sign-off"
    - path: .planning/phases/ZZZ-01-trustworthy-theory-pipeline/01-04-SUMMARY.md
      provides: "executor-generated final gate outcome"
  key_links:
    - from: package.json
      to: scripts/build-contract.js
      via: "npm run build"
      pattern: "build"
    - from: package.json
      to: tests
      via: "npm test executes complete Node test suite"
      pattern: "node --test"
    - from: playwright.config.js
      to: tests/e2e/dashboard.spec.js
      via: "npm run test:e2e runs desktop and mobile projects"
      pattern: "test:e2e|projects"
    - from: .planning/phases/ZZZ-01-trustworthy-theory-pipeline/01-VALIDATION.md
      to: experiments/output/theory/zzz-1-4-fade-run.json
      via: "closure evidence cites regenerated READY artifact validation"
      pattern: "READY_FOR_COMPILATION|TRUST-03"
  prohibitions:
    - id: PROHIB-TRUST-09
      category: transparency
      status: unresolved
      verification: null
      statement: "MUST NOT mark Phase 1 complete with a skipped command, expected-failure waiver, weakened locked test, unresolved HIGH security finding, or dirty product worktree."
    - id: PROHIB-TRUST-10
      category: safety
      status: unresolved
      verification: null
      statement: "MUST NOT deploy an Injective contract, switch mainnet, read a private key, initiate a transaction, or invoke a live model during closure verification."
    - id: PROHIB-TRUST-11
      category: values
      status: unresolved
      verification: null
      statement: "MUST NOT describe synthetic groups as real player samples or scenario risk indices as real-world probabilities in any changed artifact."
  flagged_assumptions:
    - "EP-01 flagged/unverified: TRUST-01 was unclassified; closure assumes its exact observable contract is the unchanged six-test CLI suite plus safe filesystem diff review."
    - "EP-02 flagged/unverified: TRUST-02 was unclassified; closure assumes three-mode provenance tests and regenerated fixture fields are sufficient Phase 1 evidence."
    - "EP-03 flagged/unverified: TRUST-03 was unclassified; closure assumes READY reconstruction against current inputs is the authority criterion."
    - "EP-04 flagged/unverified: TRUST-04 numeric boundary is non-applicable because commands expose exact zero/non-zero exit status, not min/max/threshold values."
    - "EP-05 flagged/unverified: TRUST-04 adjacency/equality is non-applicable because any single failed gate blocks completion even when neighboring gates pass."
    - "EP-06 flagged/unverified: TRUST-04 empty/single/null is non-applicable because every named command must produce an observed exit status; absent evidence is failure."
    - "EP-07 flagged/unverified: TRUST-04 equal-element ordering is non-applicable because no collection is sorted; the fixed gate order is focused tests, build, full Node, then E2E."
    - "EP-08 flagged/unverified: TRUST-04 precision/overflow/rounding is non-applicable because exit codes and SHA-256 strings are exact and never rounded."
    - "Worktree assumption flagged/unverified: `.codex/` is pre-existing untracked local GSD runtime; clean product-worktree checks must exclude only that known tooling path and block every other unexpected tracked/untracked product path."
    - "Platform assumption locked: macOS is the only pre-competition supported platform; non-macOS support and cross-platform verification are explicitly post-competition."
    - "Canon breadcrumb: path traversal, symlink, injection, and generic credential disclosure are handled by the STRIDE review, secret scan, and $gsd-secure-phase/security review; they are not minted as bespoke prohibitions."
---

<objective>
在比赛 macOS 机器上用完整的 build、Node、desktop/mobile viewport E2E、diff、secret、claim-boundary 和 ASVS L2 检查关闭 Phase 1，并把不可跳过的执行证据写回 validation contract。

Purpose: 只有整个本地质量门禁完全绿色且没有 HIGH 安全问题时，才能宣称可信 Theory pipeline 已恢复。
Output: 完整绿色门禁与可审计 closure evidence。
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/ZZZ-01-trustworthy-theory-pipeline/01-CONTEXT.md
@.planning/phases/ZZZ-01-trustworthy-theory-pipeline/01-VALIDATION.md
@.planning/phases/ZZZ-01-trustworthy-theory-pipeline/01-03-SUMMARY.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONCERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Recheck focused trust contracts and regenerated READY authority</name>
  <read_first>
    - @.planning/phases/ZZZ-01-trustworthy-theory-pipeline/01-03-SUMMARY.md
    - @tests/theory-agent-cli.test.js
    - @tests/theory-mapper.test.js
    - @tests/theory-agent-security.test.js
    - @tests/theory-agent.test.js
    - @tests/compile-scenario-cli.test.js
    - @experiments/output/theory/zzz-1-4-fade-run.json
    - @.planning/phases/ZZZ-01-trustworthy-theory-pipeline/01-VALIDATION.md
  </read_first>
  <precondition>The regenerated default run exists, reports READY_FOR_COMPILATION, and Plan 01-03 committed all intended product artifacts.</precondition>
  <files>.planning/phases/ZZZ-01-trustworthy-theory-pipeline/01-VALIDATION.md</files>
  <action>Run the four-file Theory command and `tests/compile-scenario-cli.test.js`; verify the six locked CLI cases all pass and `tests/theory-agent-cli.test.js` has not been weakened. Re-run CLI `status` and confirm the checked run's current input digests, deterministic fixture path/digest, false model capability, review/event lineage, and content-addressed Theory System reconstruct cleanly. Record exact commands and green status in `01-VALIDATION.md`; do not fabricate test counts or mark Nyquist complete yet. This is the focused portion of D-10 and D-11.</action>
  <verify>
    <automated>node --test tests/theory-mapper.test.js tests/theory-agent.test.js tests/theory-agent-security.test.js tests/theory-agent-cli.test.js tests/compile-scenario-cli.test.js && node -e "const c=require('node:crypto');const f=require('node:fs');process.exit(c.createHash('sha256').update(f.readFileSync('tests/theory-agent-cli.test.js')).digest('hex')==='53ca63c48afb367691caa492cb478823575473e922719e9cafd63c97effe8e23'?0:1)" && npm run theory:agent -- status</automated>
  </verify>
  <acceptance_criteria>Focused trust tests and READY status are green, locked attacks remain intact, and validation evidence contains actual command outcomes only.</acceptance_criteria>
  <done>The phase-specific trust surface is green before full repository commands begin.</done>
</task>

<task type="auto">
  <name>Task 2: Run the mandatory build, complete Node, and desktop/mobile E2E gates</name>
  <read_first>
    - @package.json
    - @package-lock.json
    - @playwright.config.js
    - @tests/e2e/dashboard.spec.js
    - @.planning/codebase/TESTING.md
    - @.planning/phases/ZZZ-01-trustworthy-theory-pipeline/01-VALIDATION.md
  </read_first>
  <files>.planning/phases/ZZZ-01-trustworthy-theory-pipeline/01-VALIDATION.md</files>
  <action>On the current competition macOS machine, first run `npm ci` to restore the exact `package-lock.json` environment required by `solc` and `@playwright/test`; this lockfile restoration is required pre-gate setup, not a dependency addition. Confirm `package.json` and `package-lock.json` remain unchanged, and do not run any package-add/update command. Then execute `npm run build`, `npm test`, and `npm run test:e2e` as three explicit commands in that order per D-10. Stop immediately on any non-zero exit and do not add an expected-failure waiver. Do not invoke a live model or initiate any wallet/Injective action. Record the actual `npm ci` result and each gate result, including both configured viewport projects, in `01-VALIDATION.md` without recalled numeric baselines. This is target-platform evidence, not a cross-platform claim.</action>
  <verify>
    <automated>npm ci && npm run build && npm test && npm run test:e2e</automated>
  </verify>
  <acceptance_criteria>Exact-lockfile restoration succeeds without manifest/lockfile changes; all three mandatory repository commands exit 0 in the required order and desktop/mobile E2E both execute.</acceptance_criteria>
  <done>TRUST-04's complete local quality gate is green with no waiver.</done>
</task>

<task type="auto">
  <name>Task 3: Perform diff, secret, claim-boundary, and ASVS L2 closure audit</name>
  <read_first>
    - @.planning/phases/ZZZ-01-trustworthy-theory-pipeline/01-VALIDATION.md
    - @docs/restart-handoff-2026-07-24.md
    - @docs/mvp-v1-spec.md
    - @README.md
    - @research/README.md
    - @.planning/phases/ZZZ-01-trustworthy-theory-pipeline/01-CONTEXT.md
  </read_first>
  <files>.planning/phases/ZZZ-01-trustworthy-theory-pipeline/01-VALIDATION.md</files>
  <action>Run `git diff --check`, inspect the complete Phase 1 diff for unintended paths and weakened tests, and scan executable/data/generated paths for private-key, credential, mnemonic, or raw model-secret material without printing secret values. Verify changed prose retains public-source bindings, calls fixture runs non-live, never calls synthetic representatives player samples, and never calls scenario indices probabilities. Verify filesystem prose states the cooperative-writer `flock` boundary and same-UID non-cooperative residual risk without universal "never" claims. Apply the plan threat registers at OWASP ASVS L2 and block closure on any unresolved HIGH-or-above issue inside the approved boundary. Reconcile `01-VALIDATION.md` against exactly the nine mapped task IDs and attach actual evidence to every row. Only after all nine rows are green, Wave 0 test references exist and pass, `npm ci` plus all mandatory focused/build/full-Node/desktop-mobile-viewport E2E gates have recorded zero exits, and every diff/secret/claim/ASVS review is green may the task set `wave_0_complete: true`, `nyquist_compliant: true`, `status: complete`, and `Approval: approved`; any missing, skipped, red, flaky, or waived evidence must leave the corresponding row non-green and the phase at `false`/`draft`/`pending`. Preserve the `.codex/` local-tooling assumption explicitly and require no other unexpected product-worktree entry. Record PR-first ordering, but do not push or open the PR from this plan. This completes D-10 and D-11.</action>
  <verify>
    <automated>git diff --check && if git grep -lE '(BEGIN (RSA|OPENSSH|EC) PRIVATE KEY|sk-[A-Za-z0-9]{20,}|0x[0-9a-fA-F]{64})' -- app.js scripts src data experiments contracts tests ':(exclude)src/contract-artifact.js'; then exit 1; fi</automated>
  </verify>
  <acceptance_criteria>No diff defect, credential/private-key hit, unsupported claim, unexpected product path, weakened locked test, or unresolved HIGH finding remains; validation frontmatter and rows reflect actual green evidence.</acceptance_criteria>
  <done>Phase 1 has a completely green, security-reviewed, claim-safe local closure record and is ready for `$gsd-verify-work`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| changed repository -> build/test runners | Generated code, tests, and browser artifacts must all agree after the trust changes. |
| changed files -> public repository | Secrets, unsupported claims, or stale authority text must not leave the local boundary. |
| command results -> phase status | Missing, skipped, or selectively reported failures must not become a green completion claim. |
| local writers -> run transaction | Project writers cooperate through the parent-directory flock; non-cooperating same-UID mutation is an explicit residual risk. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-CLOSE-T | Tampering | locked tests and generated run | high | mitigate | Review complete diff, rerun focused/full suites, and reject any weakened attack or manually edited content identity. |
| T-01-CLOSE-I | Information Disclosure | public diff | high | mitigate | Run targeted secret scan and inspect errors/artifacts for credential, private-key, mnemonic, or parser-content leakage. |
| T-01-CLOSE-R | Repudiation | validation evidence | medium | mitigate | Record exact commands/results and require all three mandatory gates before setting Nyquist/sign-off green. |
| T-01-CLOSE-S | Spoofing | phase completion status | high | mitigate | Block on any failed/skipped gate, dirty product path, stale READY artifact, or unresolved HIGH finding. |
| T-01-CLOSE-D | Denial of Service | full local gate runtime | low | accept | Commands are bounded local CI equivalents; a timeout/failure leaves the phase incomplete rather than changing product state. |

</threat_model>

<verification>
The final executor must show green output on the competition macOS machine for exact-lockfile restoration with `npm ci`, focused tests, `npm run build`, `npm test`, `npm run test:e2e`, `git diff --check`, secret scan, claim-boundary review, and ASVS L2 review. Any missing result is a failure; this evidence does not constitute cross-platform verification.
</verification>

<success_criteria>
- D-10 and D-11 are satisfied exactly with no expected-failure waiver.
- All TRUST-01 through TRUST-04 acceptance behavior is evidenced in automated commands.
- No HIGH security issue or secret/claim boundary violation remains.
- `01-VALIDATION.md` is Nyquist-compliant only after real green evidence exists.
</success_criteria>

## Artifacts this phase produces

- Updated `.planning/phases/ZZZ-01-trustworthy-theory-pipeline/01-VALIDATION.md` frontmatter (`wave_0_complete`, `nyquist_compliant`, `status`) and per-task/command evidence fields.
- Executor-generated `.planning/phases/ZZZ-01-trustworthy-theory-pipeline/01-04-SUMMARY.md` with final gate outcomes.
- No new package, schema, model output, research conclusion, wallet state, contract deployment, or transaction.

## Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | - | Safely generate and compile an authoritative reviewed Theory System with a green local gate | 01-01..01-04 | COVERED | Tracer, hardening, regeneration, closure form one dependency chain. |
| REQ | TRUST-01 | Prevent output escape or overwrite of reviewed inputs | 01-02, 01-04 | COVERED | Six locked CLI attacks plus full closure. |
| REQ | TRUST-02 | Distinguish fixture, recorded, and live output with complete provenance | 01-01, 01-04 | COVERED | Closed mode validator plus persisted/System checks. |
| REQ | TRUST-03 | Regenerate READY current lineage and digests | 01-03, 01-04 | COVERED | CLI-only lifecycle and reconstruction. |
| REQ | TRUST-04 | Build, complete Node, and desktop/mobile E2E gates | 01-04 | COVERED | All three mandatory commands are explicit. |
| RESEARCH | - | Closed provenance at mapper, preserve/revalidate in run/System | 01-01 | COVERED | Follows responsibility map. |
| RESEARCH | - | Port compiler filesystem pattern, safe JSON errors, locked regressions | 01-02 | COVERED | No new dependency or second design. |
| RESEARCH | - | Regenerate only after focused tests; update exact references | 01-03 | COVERED | No manual patch. |
| RESEARCH | - | Full gates, diff, secret and security closure | 01-04 | COVERED | ASVS L2 blocks HIGH. |
| CONTEXT | D-01 | Port realpath/symlink/protected-input/atomic pattern | 01-02 | COVERED | Tasks 1-2. |
| CONTEXT | D-02 | Random exclusive 0600 fsync atomic output | 01-02 | COVERED | Task 2. |
| CONTEXT | D-03 | Reject all protected aliases before provider | 01-02 | COVERED | Task 1. |
| CONTEXT | D-04 | Fixture path/digest and false capability | 01-01, 01-03 | COVERED | Tracer and regenerated artifact. |
| CONTEXT | D-05 | Attributable live mode and distinct recorded mode | 01-01 | COVERED | Mode table tests. |
| CONTEXT | D-06 | Earliest reusable fail-closed validation | 01-01 | COVERED | Mapper validator plus CLI observable test. |
| CONTEXT | D-07 | Preserve commands and env names | 01-01..01-03 | COVERED | No CLI surface change. |
| CONTEXT | D-08 | CLI lifecycle regeneration, no manual ID patch | 01-03 | COVERED | Exact ordered commands. |
| CONTEXT | D-09 | Artifact-safe redacted JSON errors | 01-02 | COVERED | Task 2. |
| CONTEXT | D-10 | Focused/build/full/E2E green | 01-04 | COVERED | Tasks 1-3. |
| CONTEXT | D-11 | Six regressions green, no weakening/waiver | 01-02, 01-04 | COVERED | Locked test remains read-only. |

Deferred real-model calls, hybrid agents, 3D channels, and Injective actions remain excluded exactly as `01-CONTEXT.md` specifies.

<output>
Create `.planning/phases/ZZZ-01-trustworthy-theory-pipeline/01-04-SUMMARY.md` when done.
</output>
